# AI エージェント向け開発ガイド

このファイルは、このリポジトリで Issue を実装する AI エージェントが従う共通ルールと、プロジェクト共通の完了条件を定義します。リポジトリ全体に適用し、より限定されたディレクトリに別の `AGENTS.md` がある場合は、その配下では両方の指示に従ってください。

## 作業を始める前に

1. 対象 Issue の本文、完了条件、対応範囲、対象外、コメント、関連資料を確認する。
2. 完了条件を検証可能な作業項目として整理してから実装を始める。
3. 既存のコード、ドキュメント、テスト、設定と作業ツリーの状態を確認する。既存の未コミット変更は、今回の変更と明確に区別して保持する。
4. 要件の不明点が成果物や設計を大きく左右する場合は、推測で進めず質問する。安全で元に戻せる小さな判断は、仮定を明示して進めてよい。

## 実装ルール

### Issue と変更範囲

- Issue の目的と全ての完了条件を満たす、必要最小限の変更を行う。
- Issue と無関係なリファクタリング、整形、依存関係の更新、機能追加は行わない。別の問題を発見した場合は、今回の実装へ混ぜずに報告する。
- Issue に明記された対象外の作業は行わない。
- 既存の命名、構成、公開インターフェースとの一貫性を保つ。

### 設計変更

- 公開インターフェース、主要な依存関係、データ形式、責務の境界、ディレクトリ構成などに大きな変更が必要になった場合は、実装を進める前に理由、選択肢、影響範囲、移行方法を整理し、Issue の依頼者に確認する。
- 合意されていない大きな設計変更を、Issue の実装に付随する変更として行わない。必要に応じて設計 Issue / RFC として分離する。
- 将来利用するかもしれない機能や抽象化を先回りして追加しない。

### ドキュメント

- 利用方法、セットアップ、公開仕様など利用者向けの情報が変わる場合は `README.md` を同じ変更内で更新する。
- AI エージェントの共通ルール、作業手順、完了条件、開発基盤ドキュメントへの導線が変わる場合は、この `AGENTS.md` を同じ変更内で更新する。
- `README.md` と `AGENTS.md` は、実装との不一致を避けるため、変更が必要になるたびに更新する。
- コードや設定だけを見れば分かる内容を重複して記載せず、時間が経っても保守できる情報を残す。

## 検証ルール

実装後は、変更内容に応じて次を実施する。

1. 変更箇所を直接確認できるテストを追加または更新し、実行する。
2. プロジェクトに用意されている formatter、lint、型検査、ビルド、テストを、変更に関係する範囲で実行する。
3. ドキュメントだけの変更では、内容、Markdown、リンク、記載したコマンドの整合性を確認する。
4. 最後に差分を自己レビューし、デバッグ出力、一時ファイル、機密情報、意図しない変更が含まれていないことを確認する。

検証手段がまだ存在しない、または実行できない場合は、実施していない項目と理由を完了報告に明記する。検証を省略したまま成功したとは扱わない。

## プロジェクト共通の完了条件

次の全項目を満たしたときに作業完了とする。

- 対象 Issue の全ての完了条件を満たしている。
- 実装が Issue の対応範囲内に収まり、対象外や無関係な変更を含んでいない。
- 必要なテストが追加または更新され、関連する検証が成功している。
- 変更に影響される `README.md`、`AGENTS.md`、その他のドキュメントが更新されている。
- 差分の自己レビューが完了し、既知の制約や未解決事項が報告されている。
- 完了報告に、変更概要、実施した検証と結果、残課題（ある場合）が記載されている。

## 開発基盤ドキュメント

作業時は、対象に応じて次の資料も参照する。新しい共通ルールや開発基盤ドキュメントを追加した場合は、ここに導線を追加する。

- [`README.md`](README.md): プロジェクト概要と開発者向けの入口
- [Issue テンプレート](.github/ISSUE_TEMPLATE): 機能追加、不具合、設計検討の起票項目と完了条件
- [Pull Request テンプレート](.github/pull_request_template.md): 概要、関連 Issue、変更内容、判断理由、完了条件への対応、検証結果、影響、未解決事項の記録
